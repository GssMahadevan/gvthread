--- a/Cargo.toml
+++ b/Cargo.toml
@@ -5,6 +5,11 @@ members = [
     # Core crates
     "crates/gvthread-core",
     "crates/gvthread-runtime",
     "crates/gvthread",
     
+    # KSVC crates (kernel syscall virtualization channel)
+    "crates/ksvc-core",
+    "crates/ksvc-module",
+    "crates/ksvc-executor",
+    
     # Optional/future crates (uncomment when ready)
     # "crates/gvthread-ipc",
     
@@ -28,6 +33,11 @@ gvthread-core = { path = "crates/gvthread-core" }
 gvthread-runtime = { path = "crates/gvthread-runtime" }
 gvthread = { path = "crates/gvthread" }
 
+# Internal — KSVC
+ksvc-core = { path = "crates/ksvc-core" }
+ksvc-module = { path = "crates/ksvc-module" }
+ksvc-executor = { path = "crates/ksvc-executor" }
+
 # External dependencies (shared versions)
 libc = "0.2"
-nix = { version = "0.27", features = ["signal", "pthread", "mman"] }
+nix = { version = "0.29", features = ["signal", "pthread", "mman", "fs", "event", "ioctl"] }
+io-uring = "0.7"
+crossbeam-queue = "0.3"
 cfg-if = "1.0"
